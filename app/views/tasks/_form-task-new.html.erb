<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3 col-md-6 col-md-offset-3">
      <div class="breath">
        <%= simple_form_for([@wedding, @task]) do |f| %>
          <%= f.input :name, label: "Nouvelle discussion, intitulée..." %>
          <%= f.submit t("task.create"), class: "button maxwidth" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
