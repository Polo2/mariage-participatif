<%= simple_form_for([@wedding, @task, @element]) do |f| %>

  <%= f.input :content, label: false, placeholder: t("task.type_here"), as: :text %>
  <%=
  # f.input :public, collection: [ public_for(true), public_for(false) ], as: :radio_buttons
  %>
  <%= f.input :public, collection: { Oui: true, Non: false}, as: :radio_buttons %>
  <%= f.button :submit, t("task.add_element") %>

<% end %>


